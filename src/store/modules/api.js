const state = {
  config: [
  ],
  config_select: {
    "id-example": {
      "options": [],
      "values": [],
    },
  },
  fields: [
    // each field will have its config and config_select
  ],
}

const mutations = {
    
}

const actions = {
  
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
};